name: test
on:
  push:
    branches:
      - main
jobs:
  test-scala:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Scala
      uses: olafurpg/setup-scala@v10
      with:
        java-version: openjdk@1.11
